# =============================================================================
# Gantt Chart Application - Environment Variables
# =============================================================================
# Bu dosyayı ana dizine .env olarak kopyalayın:
#   copy infra\env.example .env (Windows)
#   cp infra/env.example .env (Linux/Mac)
# =============================================================================

# -----------------------------------------------------------------------------
# Docker Compose
# -----------------------------------------------------------------------------
COMPOSE_PROJECT_NAME=gantt

# -----------------------------------------------------------------------------
# PostgreSQL Database
# -----------------------------------------------------------------------------
POSTGRES_DB=gantt_app
POSTGRES_USER=gantt_user
POSTGRES_PASSWORD=gantt_secret_2024
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# Flask Backend
# -----------------------------------------------------------------------------
FLASK_ENV=development
SECRET_KEY=your-super-secret-key-change-in-production-2024
JWT_EXPIRATION_HOURS=24

# Database URL (auto-composed from above values in docker-compose)
# For local development without Docker:
# DATABASE_URL=postgresql+psycopg2://gantt_user:gantt_secret_2024@localhost:5432/gantt_app

# -----------------------------------------------------------------------------
# Frontend (Vite)
# -----------------------------------------------------------------------------
VITE_API_BASE_URL=http://localhost:5000/api

# -----------------------------------------------------------------------------
# Production Settings (change these in production!)
# -----------------------------------------------------------------------------
# FLASK_ENV=production
# SECRET_KEY=<generate-a-strong-random-key>
# POSTGRES_PASSWORD=<strong-random-password>

